name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        racket-version: ['8.14', '8.15', '8.16', '8.17', '8.18', 'current']
        racket-variant: ['CS']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Racket
        uses: Bogdanp/setup-racket@v1.11
        with:
          architecture: 'x64'
          distribution: 'full'
          variant: ${{ matrix.racket-variant }}
          version: ${{ matrix.racket-version }}

      - name: Install package and dependencies
        run: |
          raco pkg install --auto --batch --name algorhythms

      - name: Build package
        run: |
          raco setup --check-pkg-deps algorhythms

      - name: Run tests
        run: |
          raco test --package algorhythms

  lint:
    name: Code Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Racket
        uses: Bogdanp/setup-racket@v1.11
        with:
          architecture: 'x64'
          distribution: 'full'
          variant: 'CS'
          version: '8.18'

      - name: Install package
        run: |
          raco pkg install --auto --batch --name algorhythms

      - name: Check syntax and compilation
        run: |
          raco setup algorhythms

      - name: Verify package dependencies
        run: |
          raco pkg show algorhythms
